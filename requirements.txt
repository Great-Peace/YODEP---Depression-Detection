# YODEP — Yoruba-English Acted Depression Speech Corpus
# Requirements with pinned versions
# Python >= 3.10 required
#
# INSTALL NOTES:
# 1. Parselmouth requires a C compiler. On Ubuntu: sudo apt-get install build-essential
#    On macOS: xcode-select --install
# 2. openSMILE is installed as a Python package (opensmile) — no separate binary needed
# 3. PyTorch: Install separately BEFORE requirements.txt for the correct CUDA build.
#    CUDA 12.x (L40, A100, H100, RTX 4xxx):
#    pip install torch==2.2.1+cu121 torchaudio==2.2.1+cu121 --index-url https://download.pytorch.org/whl/cu121
#    CUDA 11.8 (older V100, RTX 3xxx):
#    pip install torch==2.2.1+cu118 torchaudio==2.2.1+cu118 --index-url https://download.pytorch.org/whl/cu118
#    For Apple Silicon (MPS), the standard pip install below works.
# 4. transformers requires sentencepiece for some tokenizers:
#    pip install sentencepiece

# Core scientific
numpy==1.26.4
scipy==1.12.0
pandas==2.2.1
scikit-learn==1.4.1.post1

# Audio processing
librosa==0.10.1
soundfile==0.12.1
praat-parselmouth==0.4.3
opensmile==2.5.0

# Deep learning
torch==2.2.1
torchaudio==2.2.1
transformers==4.39.3
huggingface-hub==0.22.2
sentencepiece==0.2.0
tokenizers==0.15.2

# Visualization
matplotlib==3.8.3
seaborn==0.13.2

# Utilities
pyyaml==6.0.1
tqdm==4.66.2
joblib==1.3.2
click==8.1.7
colorlog==6.8.2

# Testing
pytest==8.1.1
pytest-cov==5.0.0

# Notebook
jupyter==1.0.0
ipykernel==6.29.4

# Statistics
pingouin==0.5.4
statsmodels==0.14.1

# Gradient boosting
xgboost>=2.0.0
